apiVersion: v1
kind: Service
metadata:
  name: codelens-frontend
  namespace: codelens
  labels:
    app.kubernetes.io/name: codelens
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: codelens
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
